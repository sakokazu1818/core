%p#notice= notice
%h1 Tops
%table
  %thead
    %tr
      %th{:colspan => "3"}
  %tbody
    - @tops.each do |top|
      %tr
        %td= link_to 'Show', top
        %td= link_to 'Edit', edit_top_path(top)
        %td= link_to 'Destroy', top, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Top', new_top_path
